<% content_for :title, "New Project" %>
<div class="min-h-screen flex items-center bg-gray-50">
  <div class="max-w-3xl w-full mx-auto px-4 sm:px-6 lg:px-8 animated zoomIn">
    <div class="bg-white shadow-lg rounded-lg p-8">
      <h1 class="text-4xl font-bold text-center mb-8 border-b">New Project</h1>
      <%= render "form", project: @project %>
    </div>
  </div>
</div>
